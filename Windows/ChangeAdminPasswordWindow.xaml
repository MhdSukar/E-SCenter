<Window x:Class="ESCenter.Windows.ChangeAdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Admin Credentials"
        Height="360"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="TitleBarButtonBase" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonBase}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="0,8,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#E81123"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#C50F1F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="{StaticResource AppBackgroundBrushNew}" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="0" SnapsToDevicePixels="True">
        <Grid>
            <Border Height="36" Background="{StaticResource DarkCardBrush}" CornerRadius="12,12,0,0" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" VerticalAlignment="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Source="Z:\C#\Projects\E-SCenter\E-SCenter\Themes\E-SCenter.png" Width="18" Height="18" Margin="10,0,6,0" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" Text="Change Admin Credentials" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource TitleBarButtonBase}" Click="btnMinimize_Click" x:Name="btnMinimize">
                            <Image Source="{StaticResource Minimize}" Width="14" Height="14" Stretch="Uniform"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarButtonBase}" x:Name="btnMaximize" FontSize="17" Click="btnMaximize_Click">
                            <Image Source="{StaticResource Maximize}" Width="14" Height="14" Stretch="Uniform"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarCloseButtonStyle}" Click="btnClose_Click">
                            <Image Source="{StaticResource Close}" Width="14" Height="14" Stretch="Uniform"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Margin="0,36,0,0" Padding="12" CornerRadius="0,0,10,10" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1">
                <Grid Margin="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Update Admin Username &amp; Password" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,14" Grid.Row="0"/>

                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,4">
                        <TextBlock Text="Current User:" Width="130" VerticalAlignment="Center"/>
                        <TextBox TabIndex="0" x:Name="CurrentUsernameBox" Width="220" Height="28" Style="{StaticResource NeonTextBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,4">
                        <TextBlock Text="Current Password:" Width="130" VerticalAlignment="Center"/>
                        <PasswordBox TabIndex="1" x:Name="CurrentPasswordBox" Width="220" Style="{StaticResource NeonPasswordBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,4">
                        <TextBlock Text="New Username:" Width="130" VerticalAlignment="Center"/>
                        <TextBox TabIndex="2" x:Name="NewUsernameBox" Width="220" Height="28" Style="{StaticResource NeonTextBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="0,4">
                        <TextBlock Text="New Password:" Width="130" VerticalAlignment="Center"/>
                        <PasswordBox TabIndex="3" x:Name="NewPasswordBox" Width="220" Style="{StaticResource NeonPasswordBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="5" Margin="0,4">
                        <TextBlock Text="Confirm New Pass:" Width="130" VerticalAlignment="Center"/>
                        <PasswordBox TabIndex="4" x:Name="ConfirmPasswordBox" Width="220" Style="{StaticResource NeonPasswordBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="6" HorizontalAlignment="Center" Margin="0,14,0,0">
                        <Button TabIndex="5" Content="Save" Width="100" Margin="6,0" Style="{StaticResource NeonDarkCyanButtonStyle}" Click="SaveButton_Click"/>
                        <Button TabIndex="6" Content="Cancel" Width="100" Margin="6,0" Style="{StaticResource NeonOrangeButtonStyle}" Click="CancelButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
