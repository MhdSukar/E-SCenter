<Window x:Class="ESCenter.Windows.TrayPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="240" Height="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Opacity="1">

    <Window.Resources>
        <DropShadowEffect x:Key="TrayPopupShadow"
                          Color="#99000000"
                          BlurRadius="20"
                          ShadowDepth="6"
                          Opacity="0.55"/>

        <Style x:Key="TrayActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkCardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDarkBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#262A33"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource TextLogoBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1B1F26"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="14"
            Background="{StaticResource DarkBackgroundBrush}"
            BorderBrush="{StaticResource DarkBorderBrush}"
            BorderThickness="1"
            Padding="12"
            Effect="{StaticResource TrayPopupShadow}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===== Header with exit button ===== -->
            <Border Background="{StaticResource DarkCardBrush}"
                    CornerRadius="10"
                    BorderBrush="{StaticResource BorderDarkBrush}"
                    BorderThickness="1"
                    Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="E-SCenter"
                                   VerticalAlignment="Center"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Tray shortcuts"
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,3,0,0"/>
                    </StackPanel>

                    <!-- Exit Button (icon only) -->
                    <Button Grid.Column="1"
                            Width="28" Height="28"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Command="{Binding ExitAppCommand}"
                            x:Name="BtnExit"
                            Padding="0" Margin="6,0,0,0">
                        <Image Source="{StaticResource PowerOff}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <!-- ===== Compact Buttons ===== -->
            <StackPanel Grid.Row="1" Margin="0,12,0,0" Orientation="Vertical" VerticalAlignment="Top">

                <Button Style="{StaticResource TrayActionButtonStyle}" Command="{Binding OpenDashboardCommand}" x:Name="BtnDashboard">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0" Source="{StaticResource ArmingCountdown}" />
                        <TextBlock Grid.Column="1" Text="Dashboard" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="›" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource TrayActionButtonStyle}" Command="{Binding OpenTicketsCommand}" x:Name="BtnAddTicket">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0" Source="{StaticResource regular_expression}" />
                        <TextBlock Grid.Column="1" Text="Tickets" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="›" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource TrayActionButtonStyle}" Command="{Binding OpenInventoryCommand}" x:Name="BtnInventory">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0" Source="{StaticResource inventory}" />
                        <TextBlock Grid.Column="1" Text="Inventory" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="›" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource TrayActionButtonStyle}" Command="{Binding OpenReportsCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0" Source="{StaticResource DataArray}" />
                        <TextBlock Grid.Column="1" Text="Reports" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="›" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"/>
                    </Grid>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</Window>
