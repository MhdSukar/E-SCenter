<UserControl x:Class="ESCenter.Views.PartsControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ESCenter.ViewModels"
             Height="Auto"
             Width="Auto">

    <UserControl.DataContext>
        <vm:PartsControlViewModel/>
    </UserControl.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ===================== TOOLBAR ===================== -->
        <Border Grid.Row="0"
                Padding="10"
                CornerRadius="8"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderDarkBrush}"
                BorderThickness="1"
                Margin="0,0,0,10">

            <DockPanel LastChildFill="False">

                <!-- Left: Title + Search + Filters -->
                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Left"
                            VerticalAlignment="Center">

                    <!-- Title -->
                    <TextBlock Text="Parts Control"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource NewPrimaryBlueBrush}"
                               Margin="0,0,16,0"/>

                    <!-- Search with icon -->
                    <Grid Width="260" Margin="0,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="{StaticResource search_insights}"
                               Width="16"
                               Height="16"
                               Margin="8,0,6,0"
                               VerticalAlignment="Center"/>

                        <TextBox Grid.Column="1"
                                 Padding="6,4"
                                 VerticalAlignment="Center"
                                 Style="{StaticResource NeonTextBoxStyle}"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <!-- Part Type -->
                    <TextBlock Text="Part Type:"
                               VerticalAlignment="Center"
                               Margin="6,0,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}"/>

                    <ComboBox Width="170"
                              Margin="10,0,0,0"
                              Style="{StaticResource NeonComboBoxStyle}"
                              ItemsSource="{Binding PartTypes}"
                              SelectedItem="{Binding SelectedTypeFilter}"
                              VerticalAlignment="Center"/>

                    <!-- Quantity Quick Filters -->
                    <TextBlock Text="Quantity:"
                               VerticalAlignment="Center"
                               Margin="16,0,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}"/>

                    <Button Margin="10,0,0,0"
                            Style="{StaticResource NeonGreenButtonStyle}"
                            Command="{Binding FilterAllCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource edit_audio}" Width="14" Height="14" Margin="0,0,6,0"/>
                            <TextBlock Text="All"/>
                        </StackPanel>
                    </Button>

                    <Button Margin="6,0,0,0"
                            Style="{StaticResource NeonOrangeButtonStyle}"
                            Command="{Binding FilterLowCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource Warning}" Width="14" Height="14" Margin="0,0,6,0"/>
                            <TextBlock Text="Low"/>
                        </StackPanel>
                    </Button>

                    <Button Margin="6,0,0,0"
                            Style="{StaticResource NeonBorderButtonStyle}"
                            Command="{Binding FilterOutCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource counter0}" Width="14" Height="14" Margin="0,0,6,0"/>
                            <TextBlock Text="Out"/>
                        </StackPanel>
                    </Button>

                </StackPanel>

                <!-- Right: Actions -->
                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Right"
                            HorizontalAlignment="Right">

                    <!-- Add -->
                    <Button Width="90"
                            Margin="0,0,8,0"
                            Style="{StaticResource NeonGreenButtonStyle}"
                            Command="{Binding AddPartCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource AddTask}" Width="16" Height="16" Margin="0,0,6,0"/>
                            <TextBlock Text="Add"/>
                        </StackPanel>
                    </Button>

                    <!-- Edit -->
                    <Button Width="90"
                            Margin="0,0,8,0"
                            Style="{StaticResource NeonCyanButtonStyle}"
                            Command="{Binding EditPartCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource edit_document}" Width="16" Height="16" Margin="0,0,6,0"/>
                            <TextBlock Text="Edit"/>
                        </StackPanel>
                    </Button>

                    <!-- Remove -->
                    <Button Width="90"
                            Style="{StaticResource NeonRedButtonStyle}"
                            Command="{Binding RemovePartCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{StaticResource Delete}" Width="16" Height="16" Margin="0,0,6,0"/>
                            <TextBlock Text="Remove"/>
                        </StackPanel>
                    </Button>

                </StackPanel>

            </DockPanel>
        </Border>

        <!-- ===================== GRID ===================== -->
        <Border Grid.Row="1"
                CornerRadius="8"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderDarkBrush}"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding PartsView}"
                      AutoGenerateColumns="False"
                      SelectedItem="{Binding SelectedPart, Mode=TwoWay}"
                      Style="{StaticResource DarkDataGridStyle}"
                      RowStyle="{StaticResource CompactDataGridRowStyle}"
                      CellStyle="{StaticResource CompactDataGridCellStyle}"
                      ColumnHeaderStyle="{StaticResource CompactDataGridHeaderStyle}">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120"/>
                    <DataGridTextColumn Header="Part Code" Binding="{Binding PartCode}" Width="200"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding PartType}" Width="130"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StockStatus}" IsReadOnly="True" Width="80"/>
                    <DataGridTextColumn Header="Qty"
                                        Binding="{Binding QuantityOnHand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="70"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:F2}}" Width="90"/>
                    <DataGridTextColumn Header="Quality" Binding="{Binding QualityGrade}" Width="70"/>
                    <DataGridTextColumn Header="Shelf" Binding="{Binding LocationShelf}" Width="80"/>
                    <DataGridTextColumn Header="Bin" Binding="{Binding LocationBin}" Width="80"/>
                    <DataGridTextColumn Header="Unit 1" Binding="{Binding Unit1Display}" Width="130"/>
                    <DataGridTextColumn Header="Unit 2" Binding="{Binding Unit2Display}" Width="130"/>
                    <DataGridTextColumn Header="Chip PN" Binding="{Binding ChipPartNumber}" Width="150"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="130"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="260"/>

                </DataGrid.Columns>
            </DataGrid>

        </Border>

        <!-- ===================== STATUS BAR ===================== -->
        <DockPanel Grid.Row="2"
                   Margin="4,8,4,0">

            <TextBlock Text="{Binding PartsCountDisplay}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       DockPanel.Dock="Left"/>

            <TextBlock Text=""
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Right"/>
        </DockPanel>
    </Grid>
</UserControl>
