<UserControl x:Class="ESCenter.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto"
             Width="Auto">


    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                <Image Source="{StaticResource Dashboard}"
                       Width="28"
                       Height="28"
                       Margin="0,0,8,0"/>
                <TextBlock Text="Operations Dashboard"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource NewPrimaryBlueBrush}"
                           DockPanel.Dock="Left"/>
            </StackPanel>
            

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource GradientBorderButtonStyleBLueCyan}" MinWidth="10" Width="30" Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="{StaticResource Refresh}" Width="23" Height="23"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- Main -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left column -->
            <Grid Grid.Column="0" Margin="0,0,8,0">
                <Border CornerRadius="8"
                        Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderDarkBrush}"
                        BorderThickness="1"
                        Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Recent Open Tickets"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,8"/>

                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding RecentTickets}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Style="{StaticResource DarkDataGridStyle}"
                                  RowStyle="{StaticResource TicketRowStyle}"
                                  CellStyle="{StaticResource TicketCellStyle}"
                                  ColumnHeaderStyle="{StaticResource CompactDataGridHeaderStyle}"
                                  SelectionUnit="FullRow"
                                  CanUserResizeRows="False"
                                  CanUserResizeColumns="True"
                                  GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ESC-ID" Binding="{Binding EscTicketId}" Width="82"/>
                                <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="150"/>
                                <DataGridTextColumn Header="Device" Binding="{Binding DeviceCategory}" Width="120"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding RepairStatus}" Width="130"/>
                                <DataGridTextColumn Header="Priority"
                                                    Binding="{Binding PriorityLevel}"
                                                    Width="70"
                                                    CellStyle="{StaticResource PriorityCellStyle}"/>
                                <DataGridTextColumn Header="Received"
                                                    Binding="{Binding ReceiveDate, StringFormat='yyyy-MM-dd HH:mm'}"
                                                    Width="130"/>
                                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="160"/>
                                <DataGridTextColumn Header="Final Cost" Binding="{Binding FinalCostDisplay}" Width="130"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right column -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Parts Risk Counter (replaces Priority Distribution) -->
                <GroupBox Grid.Row="0"
                          Header="Parts Risk Counter"
                          Style="{StaticResource GradientGroupBoxRed}"
                          Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock Text="Parts low stock ≤" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBox Width="56"
                                         Style="{StaticResource NeonTextBoxStyle}"
                                         Text="{Binding PartsLowStockThreshold, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                <TextBlock Text="Inventory low stock ≤" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBox Width="56"
                                         Style="{StaticResource NeonTextBoxStyle}"
                                         Text="{Binding InventoryLowStockThreshold, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
                            </StackPanel>
                        </Grid>

                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding LowStockItems}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Style="{StaticResource DarkDataGridStyle}"
                                  RowStyle="{StaticResource CompactDataGridRowStyle}"
                                  ColumnHeaderStyle="{StaticResource CompactDataGridHeaderStyle}"
                                  CanUserResizeRows="False"
                                  GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="75"/>
                                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="*"/>
                                <DataGridTextColumn Header="Item" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="50"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>

                <!-- Weekly progress -->
                <Border Grid.Row="1"
                        CornerRadius="8"
                        Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderDarkBrush}"
                        BorderThickness="1"
                        Margin="0,0,0,12"
                        Padding="12">
                    <StackPanel>
                        <TextBlock Text="Weekly Tickets Progress"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,8"/>

                        <TextBlock Text="{Binding WeeklyFinishedTickets, StringFormat=Finished: {0}}"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding WeeklyTotalTickets, StringFormat=Total this week: {0}}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,8"/>

                        <ProgressBar Minimum="0"
                                     Maximum="100"
                                     Value="{Binding WeeklyCompletionPercent}"
                                     Height="16"
                                     Style="{StaticResource ModernProgressBarStyle}"/>

                        <TextBlock Text="{Binding WeeklyCompletionPercent, StringFormat=Completion: {0:F1}%}"
                                   Margin="0,8,0,4"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>

                        <TextBlock Text="{Binding WeeklyIncome, StringFormat=Weekly income from finished tickets: {0:F2}}"
                                   Foreground="{StaticResource NeonGreenBrush}"
                                   FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
